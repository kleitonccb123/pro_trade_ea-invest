import React from 'react';\nimport { motion, AnimatePresence } from 'framer-motion';\nimport { AlertCircle, ArrowRight, Zap, TrendingUp } from 'lucide-react';\nimport {\n  Dialog,\n  DialogContent,\n  DialogHeader,\n  DialogTitle,\n  DialogDescription,\n} from '@/components/ui/dialog';\nimport { Button } from '@/components/ui/button';\nimport { useNavigate } from 'react-router-dom';\n\ninterface RobotSlotLimitModalProps {\n  isOpen: boolean;\n  onClose: () => void;\n  robotName: string;\n  currentPlanSlots: number;\n  botsUnlockedCount: number;\n  currentPlanId?: string;\n  nextPlanSlots?: number;\n}\n\nexport const RobotSlotLimitModal: React.FC<RobotSlotLimitModalProps> = ({\n  isOpen,\n  onClose,\n  robotName,\n  currentPlanSlots,\n  botsUnlockedCount,\n  currentPlanId,\n  nextPlanSlots = 0,\n}) => {\n  const navigate = useNavigate();\n\n  const handleUpgradeClick = () => {\n    navigate('/licenses');\n    onClose();\n  };\n\n  const updatesPerDay = 24;  // Rough estimate of slot usage\n  const speedMultiplier = nextPlanSlots ? ((nextPlanSlots - currentPlanSlots) / currentPlanSlots * 100).toFixed(0) : '50';\n\n  return (\n    <AnimatePresence>\n      {isOpen && (\n        <Dialog open={isOpen} onOpenChange={onClose}>\n          <DialogContent className=\"max-w-md border border-amber-500/50 bg-slate-900 shadow-2xl shadow-amber-500/20\">\n            <DialogHeader>\n              <motion.div\n                initial={{ scale: 0.8, opacity: 0 }}\n                animate={{ scale: 1, opacity: 1 }}\n                transition={{ delay: 0.1 }}\n                className=\"flex items-center gap-3 mb-2\"\n              >\n                <div className=\"p-3 bg-gradient-to-br from-orange-500 to-red-600 rounded-lg\">\n                  <AlertCircle className=\"w-6 h-6 text-white\" />\n                </div>\n                <DialogTitle className=\"text-xl font-black text-white\">\n                  Limite de Robôs Atingido\n                </DialogTitle>\n              </motion.div>\n              <DialogDescription className=\"text-slate-400 text-sm\">\n                Você desbloqueou um robô incrível, mas chegou ao limite do seu plano.\n              </DialogDescription>\n            </DialogHeader>\n\n            {/* Main Content */}\n            <motion.div \n              initial={{ opacity: 0, y: 10 }}\n              animate={{ opacity: 1, y: 0 }}\n              transition={{ delay: 0.2 }}\n              className=\"space-y-4\"\n            >\n              {/* Robô Desbloqueado */}\n              <div className=\"bg-gradient-to-r from-green-600/20 to-emerald-600/20 border border-green-500/50 rounded-lg p-4\">\n                <p className=\"text-sm text-green-400 font-semibold mb-1\">✅ Robô Desbloqueado</p>\n                <p className=\"text-lg font-black text-white\">{robotName}</p>\n              </div>\n\n              {/* Current Status */}\n              <div className=\"bg-slate-800/50 rounded-lg p-4 border border-slate-700\">\n                <p className=\"text-sm text-slate-400 font-semibold mb-3\">Status Atual</p>\n                <div className=\"space-y-2\">\n                  <div className=\"flex items-center justify-between\">\n                    <span className=\"text-slate-300\">Robôs Ativos:</span>\n                    <span className=\"text-white font-black\">{botsUnlockedCount}/{currentPlanSlots}</span>\n                  </div>\n                  <div className=\"w-full bg-slate-700 rounded-full h-2\">\n                    <motion.div\n                      initial={{ width: 0 }}\n                      animate={{ width: `${(botsUnlockedCount / currentPlanSlots) * 100}%` }}\n                      transition={{ duration: 0.5, delay: 0.4 }}\n                      className=\"h-full bg-gradient-to-r from-orange-500 to-red-600 rounded-full\"\n                    />\n                  </div>\n                </div>\n              </div>\n\n              {/* What You're Missing */}\n              <div className=\"bg-slate-800/50 rounded-lg p-4 border border-slate-700\">\n                <p className=\"text-sm text-slate-400 font-semibold mb-3 flex items-center gap-2\">\n                  <Zap className=\"w-4 h-4 text-amber-400\" />\n                  Options\n                </p>\n                <div className=\"space-y-2 text-sm text-slate-300\">\n                  <div className=\"flex items-start gap-2\">\n                    <span className=\"text-amber-400 mt-1\">•</span>\n                    <span>\n                      <strong className=\"text-white\">Desativar um robô</strong> existente para ativar este novo robô.\n                    </span>\n                  </div>\n                  <div className=\"flex items-start gap-2\">\n                    <span className=\"text-amber-400 mt-1\">•</span>\n                    <span>\n                      <strong className=\"text-white\">Fazer upgrade</strong> do seu plano para mais slots e funcionalidades.\n                    </span>\n                  </div>\n                </div>\n              </div>\n\n              {/* Plan Comparison */}\n              <motion.div\n                initial={{ opacity: 0 }}\n                animate={{ opacity: 1 }}\n                transition={{ delay: 0.3 }}\n                className=\"bg-gradient-to-r from-blue-600/20 to-indigo-600/20 border border-blue-500/50 rounded-lg p-4\"\n              >\n                <div className=\"flex items-start gap-3\">\n                  <TrendingUp className=\"w-5 h-5 text-blue-400 mt-1 flex-shrink-0\" />\n                  <div>\n                    <p className=\"text-sm text-blue-400 font-semibold mb-2\">🚀 Upgrade do Seu Plano</p>\n                    <p className=\"text-xs text-slate-300 leading-relaxed\">\n                      O próximo plano oferece <span className=\"text-blue-400 font-bold\">até {nextPlanSlots || currentPlanSlots + 5}</span> robôs simultâneos,\n                      <span className=\"text-blue-400 font-bold\"> XP Boost 1.5x</span> e muito mais!\n                    </p>\n                  </div>\n                </div>\n              </motion.div>\n            </motion.div>\n\n            {/* Actions */}\n            <motion.div\n              initial={{ opacity: 0 }}\n              animate={{ opacity: 1 }}\n              transition={{ delay: 0.4 }}\n              className=\"flex gap-3 pt-4 border-t border-slate-700\"\n            >\n              <motion.div whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} className=\"flex-1\">\n                <Button\n                  variant=\"outline\"\n                  onClick={onClose}\n                  className=\"w-full border-slate-600 text-slate-300 hover:bg-slate-800\"\n                >\n                  Depois\n                </Button>\n              </motion.div>\n              <motion.div whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }} className=\"flex-1\">\n                <Button\n                  onClick={handleUpgradeClick}\n                  className=\"w-full bg-gradient-to-r from-blue-600 to-indigo-600 hover:from-blue-500 hover:to-indigo-500 text-white font-bold flex items-center justify-center gap-2\"\n                >\n                  <span>Upgrade Agora</span>\n                  <ArrowRight className=\"w-4 h-4\" />\n                </Button>\n              </motion.div>\n            </motion.div>\n          </DialogContent>\n        </Dialog>\n      )}\n    </AnimatePresence>\n  );\n};\n\nexport default RobotSlotLimitModal;\n